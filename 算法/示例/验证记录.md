# 合成示例验证记录

验证日期：2026-09-13。环境：Python 3.12.14、NumPy 2.5.3、SciPy 1.18.1。

运行 `run_all.py`，全部五个示例通过。

| 示例 | 本次结果 |
|---|---|
| ODE 参数辨识 | 真值 (0.22, 23)，估计 (0.22037, 23.06698)，后段测试 RMSE 0.12957 |
| 功率分配 | 五种权重均满足约束，总功率最大残差约 1.14e-13 MW；两机解析解与不可行情形检查通过 |
| 因果滤波 | 观测 RMSE 0.50989，滤波后 0.19075；前缀一致性和缺测方差增长检查通过 |
| 风险路径 | 绕障最短代价 8；封闭通路后正确返回不可达 |
| 热方程 | 粗细网格最大误差分别 2.87e-4、7.47e-5；加密后误差下降 |

这些结果来自合成数据与已知解，只验证教学实现。不能据此宣称真实风电疲劳下降、实际航路最优或达到赛题实时性要求。

当前 Codex 自带 Python 缺少 SciPy，本次将 NumPy/SciPy 安装到临时目录 `/private/tmp/shumo_algorithm_deps`，没有更改系统 Python。该临时目录可能被清理；日后请按上层 README 在自己的虚拟环境安装 requirements.txt。
